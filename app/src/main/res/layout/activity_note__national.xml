<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/neartowhite"
    tools:context=".notetype.Note_National">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="15dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        tools:layout_editor_absoluteY="-43dp">

        <Button
            android:id="@+id/mathID"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:background="@drawable/shapebutton"
            android:onClick="choseField"
            android:gravity="center"
            android:text="@string/math"
            android:textColor="@color/gray"
            android:textSize="@dimen/text_done_size" />

        <Button
            android:id="@+id/pcID"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:background="@drawable/shapebutton"
            android:onClick="choseField"
            android:text="@string/phyzique"
            android:textColor="@color/gray"
            android:textSize="@dimen/text_done_size"
            android:gravity="center" />

        <Button
            android:id="@+id/svtID"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:background="@drawable/shapebutton"
            android:onClick="choseField"
            android:text="@string/svt"
            android:textColor="@color/gray"
            android:textSize="@dimen/text_done_size"
            android:gravity="center"/>
    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>